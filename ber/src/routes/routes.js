/*import { Router } from "express";
import {
    getTasks, 
    getTask, 
    saveTask,
    getTaskCount, 
    updatetask,
    deleteTask,
    updateTask
}  from '../controllers/tasks';

const taskRoutes = Router();

taskRoutes.get('/tasks', getTasks); 

taskRoutes.get('/tasks/count', getTaskCount); 

taskRoutes.get('/tasks/:id', getTask);

taskRoutes.post('/tasks/', saveTask);

taskRoutes.delete('/tasks:id', deleteTask);  

taskRoutes.put('/tasks/:id', updateTask);
*/

const taskRoutes=0;



export default taskRoutes;